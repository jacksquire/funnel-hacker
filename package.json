{
  "name": "funnel-hacker",
  "version": "0.1.0",
  "description": "CLI tool to reverse-engineer competitor funnels",
  "type": "module",
  "bin": {
    "funnel-hacker": "./dist/cli.js"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --outdir dist --target node",
    "test": "bun test",
    "walk": "bun run src/cli.ts walk",
    "ads": "bun run src/cli.ts ads"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "playwright": "^1.42.0",
    "@anthropic-ai/sdk": "^0.20.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
